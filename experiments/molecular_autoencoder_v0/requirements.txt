# Pinned-ish core deps for the molecular autoencoder prototype (CPU is fine).
# Install torch CPU wheel from the pytorch index for a small footprint:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
gemmi>=0.6
PyYAML>=6.0
pytest>=7.0
